# Metered Gate 0.3.1 发布前测试

## A. 构建与安装

- [ ] `bash build.bash --clean` 为 0 warnings / 0 errors；
- [ ] `dist/MeteredGate-0.3.1.zip` 的根目录为 `MeteredGate/`；
- [ ] ZIP 内只有主 DLL 和声明文件，没有 `0Harmony.dll`；
- [ ] 安装前彻底删除旧 `Mods/MeteredGate`；
- [ ] 只启用 Metered Gate 启动一次，保存完整 `Player.log`。

## B. 原型与高度验证

- [ ] 建造菜单中只出现一个 Metered Gate；
- [ ] 气泡提示不显示工人需求，显示 20 kW 用电；
- [ ] 模型和图标与 Flat Connector 一致；
- [ ] 四个端口可以分别切换输入/输出；
- [ ] 在允许范围下限、上限分别可建造；
- [ ] 在下限减 1、上限加 1 时预览无效且无法提交；
- [ ] 从上限前一格按 Shift 升高，越界预览无效且无法提交；
- [ ] 从下限后一格按 Shift 降低，越界预览无效且无法提交；
- [ ] 复制单栋 Metered Gate 到范围外时无法提交；
- [ ] 多选复制包含 Metered Gate 的建筑组到范围外时无法提交；
- [ ] 蓝图中包含 Metered Gate，放到范围外时无法提交；
- [ ] 移动已有 Metered Gate 到范围外时无法提交；
- [ ] 普通非 Metered Gate 建筑不受该验证器影响；
- [ ] 不触发 Mini Zipper 切带或自动连接器逻辑。

## C. 周期与配额

- [ ] Inspector 同时显示 `-30 s`、`-1 s`、`+1 s`、`+30 s` 四个周期按钮，且没有挤压、截断或重叠；
- [ ] 四个按钮分别按预期改变周期，并都通过 InputCommand 生效；
- [ ] 在 1 秒附近连续减小不会低于 1 秒，在 3600 秒附近连续增加不会超过 3600 秒；
- [ ] 新建建筑等待完整周期后才获得第一次配额；
- [ ] 配额在货物进入闸门时扣除；
- [ ] 未用额度在周期结束时重置而非累计；
- [ ] 修改周期会清空当前配额并从零计时；
- [ ] 提高配额不会在当前周期补发；
- [ ] 降低配额会立即截断当前剩余额度；
- [ ] Restart 不删除已经缓冲的物品。

## D. 电力与 Priority

- [ ] 启用且正常运行时持续消耗 20 kW；
- [ ] 缺电时不接收、不输出、周期不推进；
- [ ] 恢复供电后继续运行；
- [ ] 原生 Priority 控件可见并可修改；
- [ ] 供电不足时高 Priority 建筑优先获得电力；
- [ ] 暂停后不耗电，恢复后没有沿用旧供电授权。

## E. 物流

- [ ] 内部最多缓存一件；
- [ ] 输出堵塞时不会继续从上游抽取；
- [ ] 疏通后缓冲物品正常输出；
- [ ] 多输出之间轮询；
- [ ] 动态关闭或改变输出端口后索引不会越界；
- [ ] 多输入共享同一份总配额。

## F. 保存迁移

### 从 0.1.0/0.2.0 v1 升级

- [ ] 备份旧存档；
- [ ] 旧存档可载入；
- [ ] 周期、配额、缓冲物和轮询状态合理保留；
- [ ] 每个旧建筑新增 20 kW consumer；
- [ ] 没有 duplicate consumer、serialization 或 prototype 错误。

### v2 再保存

- [ ] 用 0.3.1 保存并退出；
- [ ] 再次载入，建筑和 consumer 均正常；
- [ ] 重复载入不会重复创建 consumer。

## G. 复制、拆除与异常路径

- [ ] 复制设置只复制周期和配额；
- [ ] 拆除带缓冲物的建筑时货物被返还；
- [ ] 删除实体后没有残留缺电通知；
- [ ] 极限配置值 1/3600 秒、1/1000 件均正常；
- [ ] 连续快速点击加减按钮不会溢出；
- [ ] 完整日志没有未处理异常或 validator 注册错误。
