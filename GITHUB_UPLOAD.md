# GitHub 0.2.0 → 0.3.0 更新说明

本次交付包含一个独立的 GitHub 更新包。它不是依赖行号和上下文的脆弱 unified diff，而是：

- 完整的 0.3.0 overlay；
- 一个带仓库身份检查、旧文件清理和静态检查的应用脚本；
- 应用后的文件清单与提交步骤。

在本地克隆的 `MeteredGate` 仓库外解压更新包，然后执行其中说明的命令。脚本会删除旧的 `src/MeteredGateHeightPolicy.cs`，覆盖正式源码与发布文档，但不会操作 `.git`、不会提交、不会推送。
