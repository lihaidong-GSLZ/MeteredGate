# Metered Gate 0.2.0

一个 `1×1`、可架高的定量平面传送带闸门。

玩家可以分别设置周期长度与每周期放行数量。周期结束时配额重置，未使用的配额不会累积；内部最多暂存一个单位，避免下游堵塞时持续从上游抽取货物。

## 主要功能

- 四个可动态配置的平面传送带端口；
- 独立设置周期与每周期配额；
- 独立高度边界限制，普通和 Shift 快速升降均会夹紧；
- 复用原版 Flat Connector 的模型与菜单图标；
- 周期与配额读条；
- 多输出简单轮询；
- 保存、载入与复制设置；
- 兼容 0.1.0 的正式建筑原型和存档。

## 安装注意

0.2.0 的安装目录必须同时包含 `MeteredGate.dll` 和 `0Harmony.dll`。建议删除旧版目录后再复制新版本。

## 兼容性

已验证 Captain of Industry `0.8.6c`。存档中仍有 Metered Gate 建筑时不要移除模组，升级前建议备份存档。
