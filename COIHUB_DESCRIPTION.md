# Metered Gate

一个 `1×1`、可架高的定量平面传送带闸门。

玩家可以分别设置周期长度与每周期放行数量。周期结束时，配额被重置为设定值，未使用的配额不会累积。内部最多暂存一个单位，因此即使下游堵塞，也不会持续从上游抽取货物。

## 功能

- 四个可动态配置的平面传送带端口；
- 可架高；
- 可调整周期长度；
- 可调整每周期放行数量；
- 周期与配额读条；
- 多输出简单轮询；
- 保存、载入与复制设置；
- 可加入现有存档。

## 兼容性

已验证 Captain of Industry `0.8.6c`。

存档中仍有 Metered Gate 建筑时，不要移除模组。

## 安全扫描说明

模组使用反射复制原版 Flat Connector 的图形配置。它不使用 Harmony，不访问网络，也不读写任意外部文件。
